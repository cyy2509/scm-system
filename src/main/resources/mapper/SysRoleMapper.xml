<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.scm.mapper.SysRoleMapper">

    <select id="findAll" resultType="com.xxx.scm.entity.SysRole">
        SELECT * FROM sys_role ORDER BY role_id
    </select>

    <select id="findById" resultType="com.xxx.scm.entity.SysRole">
        SELECT * FROM sys_role WHERE role_id = #{roleId}
    </select>

    <select id="findByPage" resultType="com.xxx.scm.entity.SysRole">
        SELECT * FROM sys_role
        <where>
            <if test="keyword != null and keyword != ''">
                AND role_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY role_id
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="findCount" resultType="int">
        SELECT COUNT(*) FROM sys_role
        <where>
            <if test="keyword != null and keyword != ''">
                AND role_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="roleId">
        INSERT INTO sys_role(role_name, role_desc)
        VALUES(#{roleName}, #{roleDesc})
    </insert>

    <update id="update">
        UPDATE sys_role
        <set>
            <if test="roleName != null">role_name = #{roleName},</if>
            <if test="roleDesc != null">role_desc = #{roleDesc},</if>
        </set>
        WHERE role_id = #{roleId}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_role WHERE role_id = #{roleId}
    </delete>
</mapper>
