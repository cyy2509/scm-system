<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.scm.mapper.NewsMapper">

    <select id="findById" resultType="com.xxx.scm.entity.News">
        SELECT * FROM news WHERE news_id = #{newsId}
    </select>

    <select id="findByPage" resultType="com.xxx.scm.entity.News">
        SELECT * FROM news
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                     OR content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="findCount" resultType="int">
        SELECT COUNT(*) FROM news
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                     OR content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="newsId">
        INSERT INTO news(title, content, author)
        VALUES(#{title}, #{content}, #{author})
    </insert>

    <update id="update">
        UPDATE news
        SET title = #{title}, content = #{content}
        WHERE news_id = #{newsId}
    </update>

    <delete id="deleteById">
        DELETE FROM news WHERE news_id = #{newsId}
    </delete>
</mapper>
